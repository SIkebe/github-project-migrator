namespace Gpm.Browser.Tests;

/// <summary>
/// Placeholder until browser automation lands in M6 (see docs/BROWSER_AUTOMATION_PLAN.md).
/// Keeps the test project non-empty so `dotnet test` stays quiet in CI.
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void Browser_test_project_is_wired_up() => Assert.True(true);
}
